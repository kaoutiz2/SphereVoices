{#
/**
 * @file
 * Template personnalisé pour le bloc de branding.
 * Affiche uniquement le logo, masque le nom du site.
 * Le wrapper .site-logo-straddle permet de positionner le logo à cheval menu/page (style FOX News).
 */
#}
{% extends "block.html.twig" %}
{% set attributes = attributes.addClass('site-branding') %}

{% block content %}
  {% if site_logo %}
  <div class="site-logo-straddle">
    <a href="/" rel="home" class="site-branding__logo">
      <img src="{{ site_logo }}" alt="{{ 'Home'|t }}" fetchpriority="high" />
    </a>
  </div>
  {% endif %}
  {# Masquer le nom du site (site_name) #}
  {# {% if site_name %}
    <div class="site-branding__text">
      <div class="site-branding__name">
        <a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">{{ site_name }}</a>
      </div>
    </div>
  {% endif %} #}
{% endblock %}

