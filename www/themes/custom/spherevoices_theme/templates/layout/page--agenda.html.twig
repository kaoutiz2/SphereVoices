{#
/**
 * @file
 * Template pour la page agenda avec navigation Drupal.
 */
#}
{% include '@spherevoices_theme/layout/ad-banner-header.html.twig' %}
<div class="page-wrapper">
  <header class="site-header" role="banner">
    {% if page.header_top %}
      <div class="header-top">
        {{ page.header_top }}
      </div>
    {% endif %}
    <div class="header-main">
      <div class="header-main-inner site-content-width">
        <div class="header-left-section" style="display: flex !important; flex-direction: row !important; align-items: center !important; flex-wrap: nowrap;">
        <div class="header-logo-nav-row">
          {% if page.header %}
            <div class="site-branding" style="flex: 0 0 100px !important; width: 100px !important; min-width: 100px !important;">
              {{ page.header }}
            </div>
          {% endif %}
          {% if page.navigation %}
            <nav class="main-navigation mobile-menu-hidden" role="navigation" aria-label="{{ 'Navigation principale'|t }}">
              {{ page.navigation }}
            </nav>
          {% endif %}
        </div>
        <button class="menu-toggle" aria-label="{{ 'Ouvrir ou fermer le menu'|t }}" aria-expanded="false">
          <span class="menu-toggle-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M3 12H21M3 6H21M3 18H21" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </span>
        </button>
      </div>
      <div class="header-right-section">
        <div class="header-search">
          <form action="/search" method="get" class="search-form-inline">
            <input type="search" name="keys" placeholder="Rechercher..." class="search-input-inline" />
            <button type="submit" class="search-button-inline" aria-label="{{ 'Rechercher'|t }}">
              <svg class="search-button-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                <path d="M15.5 14h-.79l-.28-.27A6.471 6.471 0 0016 9.5 6.5 6.5 0 109.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z" fill="currentColor"/>
              </svg>
            </button>
          </form>
        </div>
        <div class="header-user-section">
          <div class="header-user-links">
            {% if page.header_user %}
              {{ page.header_user }}
            {% endif %}
          </div>
        </div>
      </div>
      </div>
    </div>
    <div class="header-subnav" role="navigation" aria-label="{{ 'Sous-navigation'|t }}">
      <div class="header-subnav-inner site-content-width">
        {{ page.header_subnav }}
      </div>
    </div>
  </header>

  {% if page.highlighted %}
    <div class="highlighted">
      {{ page.highlighted }}
    </div>
  {% endif %}
  
  <main class="main-content site-content-width" role="main">
    {% if page.breadcrumb %}
      {{ page.breadcrumb }}
    {% endif %}
    
    <div class="content-wrapper">
      <div class="content-main">
        <article class="content-article">
          <div class="page-header">
            <h1 class="page-title">Agenda des événements</h1>
          </div>

          <div class="page-content">
            {# Contenu agenda en HTML brut #}
            <div class="page-agenda-container">
              <p class="page-subtitle">Retrouvez tous les événements à venir</p>

              <div class="search-form">
                <form method="get" action="/agenda" id="agenda-search-form">
                  <div class="form-row">
                    <div class="form-item">
                      <label for="title">Rechercher un événement</label>
                      <input type="text" name="title" id="title" value="{{ title_search|default('') }}" placeholder="Rechercher par titre..." autocomplete="off">
                    </div>
                    
                    <div class="form-item">
                      <label for="month">Mois</label>
                      <select name="month" id="month" class="agenda-month-select">
                        <option value="">{{ 'Tous les mois'|t }}</option>
                        {% for opt in agenda_month_select_options|default([]) %}
                          <option value="{{ opt.value }}"{% if month_filter|default('') == opt.value %} selected{% endif %}>{{ opt.label }}</option>
                        {% endfor %}
                      </select>
                    </div>
                    
                    <div class="form-actions">
                      <button type="submit" class="btn-search">Rechercher</button>
                      <button type="button" class="reset-btn" id="reset-search">Réinitialiser</button>
                    </div>
                  </div>
                </form>
              </div>
              
              <script>
                (function() {
                  // Réinitialiser le formulaire
                  document.getElementById('reset-search').addEventListener('click', function(e) {
                    e.preventDefault();
                    // Rediriger directement vers /agenda (sans soumettre le formulaire)
                    window.location.replace('/agenda');
                  });
                })();
              </script>

              <div class="events-grid">
                {{ events_html|raw }}
              </div>

              <div class="agenda-footer-count">
                <p>Nombre d'événements affichés : <strong>{{ count|default(0) }}</strong></p>
              </div>
            </div>
          </div>
        </article>
      </div>
    </div>
  </main>

  {% include '@spherevoices_theme/layout/footer-site.html.twig' %}
</div>

